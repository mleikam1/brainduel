/// Firebase removed for starter MVP.
/// Keep this as a placeholder for later Remote Config keys.
class RemoteConfigKeys {
  static const categoriesManifestPath = 'categories_manifest_path';
  static const categoriesManifestVersion = 'categories_manifest_version';
  static const featuredPackPath = 'featured_pack_path';
  static const featuredPackVersion = 'featured_pack_version';
  static const defaultSessionSize = 'default_session_size';
  static const modesEnabled = 'modes_enabled';
}
